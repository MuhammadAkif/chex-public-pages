NEXT_PUBLIC_SERVER_URL=http://localhost:3000
PAYLOAD_SECRET=replace-with-a-long-random-secret

POSTGRES_DB=payload_app
POSTGRES_USER=payload
POSTGRES_PASSWORD=payload
POSTGRES_PORT=5433

# Local dev (no Docker): connect via the host-mapped port.
DATABASE_URL=postgresql://payload:payload@localhost:5433/payload_app

# Docker: the app container connects to the postgres service over the internal
# network. Override DATABASE_URL in docker-compose.yml — do NOT set it here
# for Docker, because the docker-compose.yml already sets the correct value.

# Optional port to expose the app on when running via Docker (default: 3000).
APP_PORT=3000

S3_BUCKET=your-s3-bucket-name
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
# Optional: use this when serving media through CloudFront or another public CDN.
# S3_PUBLIC_URL=https://cdn.example.com
